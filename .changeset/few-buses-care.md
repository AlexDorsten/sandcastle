---
"@ai-hero/sandcastle": patch
---

Add a host-backed `codexAppServer()` agent provider that bridges to `codex app-server`, and allow `run()` plus `wt.run()` to accept `noSandbox()` for subscription-backed Codex harness flows.
