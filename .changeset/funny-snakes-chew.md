---
"@ai-hero/sandcastle": patch
---

Add `dockerIsolated({ extraCopies })` for copying additional host files or directories into isolated Docker sandboxes, and document that `copyToWorktree` remains the right tool for repo-relative extras.
