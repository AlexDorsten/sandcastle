---
"@ai-hero/sandcastle": patch
---

Add a dedicated Codex app-server HOWTO covering host-backed `codexAppServer()` runs, sandboxed `codexCliMounts()` usage, and setup guidance for choosing between the two.
