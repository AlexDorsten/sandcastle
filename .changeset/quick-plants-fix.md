---
"@ai-hero/sandcastle": patch
---

Avoid duplicate surfaced errors from the host-backed `codexAppServer()` bridge, and clarify in the README that `wt.run()` also supports host-backed runs via `noSandbox()`.
