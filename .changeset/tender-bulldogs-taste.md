---
"@ai-hero/sandcastle": patch
---

Improve Codex-in-sandbox setup by exporting `codexCliMounts()`, updating Codex scaffolding to mount host `~/.codex` auth/config files, and switching scaffolded Codex env docs to `OPENAI_API_KEY`.
